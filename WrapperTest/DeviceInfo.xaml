<Page
    x:Class="MbientLab.MetaWear.Test.DeviceInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MbientLab.MetaWear.Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Page.Resources>
        <local:ConsoleLineColorConverter x:Key="consoleColorConverter" SevereColor="Red" InfoColor="White" />
    </Page.Resources>
    
    <Grid>
        <ListView x:Name="outputListView" Height="460" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock x:Name="outputLine" Text="{Binding Value}" HorizontalAlignment="Stretch" TextWrapping="Wrap" 
                        Foreground="{Binding Level, Converter={StaticResource consoleColorConverter}}"
                        VerticalAlignment="Top" FontSize="24" Margin="10,10,0,0" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
